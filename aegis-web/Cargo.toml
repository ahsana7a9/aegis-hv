[dependencies]
aegis-common = { path = "../aegis-common" }
axum = "0.7"
tokio = { version = "1.42", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
tower-http = { version = "0.5", features = ["cors"] } # Required for the Frontend
